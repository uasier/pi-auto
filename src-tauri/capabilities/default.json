{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open"
  ]
}
